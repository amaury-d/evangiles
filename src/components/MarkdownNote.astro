---
import { micromark } from "micromark";

const { note, as = "li", class: className = "markdown-note" } = Astro.props;
const Tag = as;
const html = micromark(note, { allowDangerousHtml: false });
---

<Tag class={className} set:html={html} />
